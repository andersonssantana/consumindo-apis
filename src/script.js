const callEndpoint = () => {
  // consumir api
  // https://brasilapi.com.br/api/ddd/v1/"??"
};

const generateResult = () => {
  // transformar os dados da api em coisas que front-end faz
};

window.onload = () => {
  // chamar tudo isso quando a página carregar
};
